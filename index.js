 const overviewElips = (str, num)=>{
            if(str?.length > num){
                  return str.slice(0, num) + "...";
            }else{
                  return str;
            }
      }
      
overviewElips(textHere, how many chartcter you wanna show num here)
